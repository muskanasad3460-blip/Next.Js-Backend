// import express from "express";
// import cors from "cors";

// const app = express();

// use morgan
// use prisma with postgresSQL using docker compose instead of mongo db.
// use express-async-handler
// make seperate classes for services and import them in controllers
// passport js with jwt and cookies strategy both
// use winston

// app.use(cors());
// app.use(express.json());

// app.get("/", (req, res) => {
//   res.send("API is running");
// });
// export default app;
